pipeline {
    agent any
    stages {
        stage('Git Version Checkout') {
            steps { 
                echo 'Git Installing'
                sh 'git --version'
            }
        }
    }
}
